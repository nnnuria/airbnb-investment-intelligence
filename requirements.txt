# ── Core data stack ───────────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet read/write

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20
#kaleido>=0.2           # static image export for plotly

# ── Statistics / ML ───────────────────────────────────────────────────────────
scipy>=1.13
scikit-learn>=1.4
statsmodels>=0.14
shap>=0.44             # model explainability (SHAP) for the price/sale models

# ── NLP / sentiment ───────────────────────────────────────────────────────────
# textblob>=0.18
# langdetect>=1.0.9
# deep-translator>=1.11
# tqdm>=4.66
# wordcloud>=1.9

# ── Data collection (Idealista scraper via Apify) ─────────────────────────────
apify-client>=1.7
python-dotenv>=1.0
xgboost>=1.7.6
lightgbm>=3.3.2

# ── API service ───────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
joblib>=1.3
httpx>=0.27          # agent layer calls FastAPI endpoints over HTTP

# ── UI (Streamlit demo app) ───────────────────────────────────────────────────
streamlit>=1.32

# ── Agent layer (RAG + LangGraph coordinator) ─────────────────────────────────
langchain-core>=0.3
langchain-community>=0.3      # FAISS / PyPDFLoader / HuggingFaceEmbeddings (regulatory agent)
langchain-text-splitters>=0.3
langchain-google-genai>=2.0   # Gemini narration (regulatory + market analyst agents)
faiss-cpu>=1.8
sentence-transformers>=3.0    # backs HuggingFaceEmbeddings
pypdf>=4.0                    # PyPDFLoader
langgraph>=0.2                # coordinator

# ── Notebook runtime ──────────────────────────────────────────────────────────
#jupyter>=1.1
#ipykernel>=6.29
#nbformat>=5.10
